global:

  scrape_interval: 10s # Check the server's health every 10 seconds



scrape_configs:

  - job_name: 'ubuntu_vm_metrics'

    static_configs:

      - targets: ['192.168.180.128:9100'] # This is where Node Exporter lives
